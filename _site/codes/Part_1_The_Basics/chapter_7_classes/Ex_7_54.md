### Exercise 7.54: Should the members of Debug that begin with set_ be declared as constexpr? If not, why not?    constexpr types are implicitly constant, so we cannot declare a function as constexpr which changes the state of a obj. 